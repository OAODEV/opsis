spec:
  template:
    spec:
      containers:
      - name: service_name
        volumeMounts:
        - mountPath: /app/charts
          name: charts-volume
      volumes:
      - name: charts-volume
        configMap:
          name: charts